/* ENUMS */
export enum OrderDirection {
  buy, sell,
}

export enum OrderType {
  stop, market, limit,
}

export enum OrderContext {
  exchange, margin,
}

export enum OrderTab {
  orders, positions
}
